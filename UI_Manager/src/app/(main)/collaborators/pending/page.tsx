import TableContent from '@/components/collaborators/pending/TableContent';

export default function PendingCollaboratorsPage() {
  return <TableContent title="Danh sách cộng tác viên mới đăng ký" />;
} 